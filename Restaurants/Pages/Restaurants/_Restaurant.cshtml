@using Core

@model RestaurantClass

<div class="card mt-3">
  <div class="card-body">
      <h5 class="card-title">@Model.Name</h5>
      <h6 class="card-subtitle mb-2 text-muted">@Model.Location</h6>
      <p class="card-text">Cuisine: @Model.Cuisine</p>
  </div>
  <div class="card-footer">
      <a class="btn btn-default" asp-page="./Detail" asp-route-restaurantId="@Model.ID"><i class="fas fa-info-circle"></i></a>
      <a class="btn btn-default" asp-page="./Edit" asp-route-restaurantId="@Model.ID"><i class="fas fa-edit"></i></a>
      <a class="btn btn-default" asp-page="./Delete" asp-route-restaurantId="@Model.ID"><i class="fas fa-trash-alt"></i></a>
  </div>
</div>